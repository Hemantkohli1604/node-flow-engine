{
  "name": "flowengine",
  "version": "1.0.0",
  "description": "A TS based  flow engine",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "npm run build"
  },
  "author": "Hemant Kohli",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.3.3"
  }
}
